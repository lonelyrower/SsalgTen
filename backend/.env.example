# 服务器配置
NODE_ENV=development
PORT=3001
HOST=localhost
# 外部访问URL（用于生成节点安装脚本，留空则自动从请求头推导）
# PUBLIC_URL=http://your-server-ip:3001

# 数据库配置
DATABASE_URL="file:./dev.db"
# DATABASE_URL="postgresql://username:password@localhost:5432/ssalgten"

# JWT 配置
# SECURITY WARNING: In production, JWT_SECRET MUST be:
# - At least 32 characters long
# - Randomly generated (use: openssl rand -base64 48)
# - Not contain weak values like "secret", "changeme", "default-secret", etc.
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-environment-must-be-32chars
JWT_EXPIRES_IN=10m
REFRESH_TOKEN_EXPIRES_IN=7d

# API 安全配置
# SECURITY WARNING: In production, API_KEY_SECRET MUST NOT be weak/default values
# Generate with: openssl rand -base64 32
API_KEY_SECRET=your-api-key-secret-for-agents-change-this
# CORS配置 - 支持多种格式：
# 开发环境: http://localhost:3000,http://127.0.0.1:3000
# 生产环境: https://yourdomain.com,https://*.yourdomain.com  
# 协议通配: https://* (允许所有HTTPS域名)
# 留空时自动根据NODE_ENV选择默认配置
CORS_ORIGIN=http://localhost:3000

# 探针配置
DEFAULT_AGENT_API_KEY=default-agent-api-key-change-this-in-production
AGENT_HEARTBEAT_INTERVAL=30000
AGENT_TIMEOUT=10000
AGENT_CONTROL_PROTOCOL=http
AGENT_CONTROL_PORT=3002
AGENT_CONTROL_TIMEOUT=8000
AGENT_CONTROL_API_KEY=${DEFAULT_AGENT_API_KEY}

# 日志配置
LOG_LEVEL=debug
ENABLE_MORGAN=true

# 外部服务配置
SPEEDTEST_SERVER_ID=auto
IPERF3_DEFAULT_PORT=5201
DEFAULT_PING_COUNT=4
DEFAULT_TRACEROUTE_MAX_HOPS=30

# IP信息服务配置 (可选)
# 注册 https://ipinfo.io 获取免费API token
# 免费版本每月提供 50,000 次查询
IPINFO_TOKEN=your-ipinfo-api-token-optional
